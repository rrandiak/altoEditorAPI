application:
  jdbc:
    driver: "org.h2.Driver"
    url: "jdbc:h2:mem:altoEditorTestDb;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE"
    username: "sa"
    password: ""
  store:
    path: "/replace/with/temporary/directory/for/tests"
    pattern: "xx/xx/xx"
  auth-kramerius:
    url: "http://localhost:8080/"
  kramerius-instances:
    test:
      title: "Kramerius Test Instance"
      url: "http://localhost:8080/"
  engines:
    pero:
      exec: "python"
      entry: "/usr/local/bin/pero.py"

spring:
  jpa:
    show-sql: false
    properties:
      hibernate:
        format_sql: false
        use_sql_comments: false
logging:
  level:
    org.hibernate.SQL: off
    org.hibernate.type.descriptor.sql.BasicBinder: off
    org.hibernate.orm.jdbc.bind: off
    org.hibernate: warn